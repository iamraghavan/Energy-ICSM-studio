import { Card, CardHeader, CardTitle, CardDescription } from "@/components/ui/card";

export default function DashboardFixturesPage() {
    return (
        <Card>
            <CardHeader>
                <CardTitle>Fixtures</CardTitle>
                <CardDescription>Create and manage match fixtures. This section is under construction.</CardDescription>
            </CardHeader>
        </Card>
    );
}
